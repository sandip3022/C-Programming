#include <stdio.h>

int main() 
{
	int num, reverse = 0, rem;
	num=6547;
    printf("The number is:%d\n",num);
	
	while(num!=0)
	{
		rem = num % 10;
		reverse = reverse * 10 + rem;
		num/=10;
	};
	
	printf("Reverse Number:%d\n", reverse);
	
	return 0;
}
